<Window x:Class="XCLing.Wpf.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="{Binding AppName}" Width="1100" Height="700" MinWidth="760" MinHeight="480" WindowStartupLocation="CenterScreen" Background="{DynamicResource Surface0Brush}" Foreground="{DynamicResource Text1Brush}" FontFamily="{StaticResource PgFontFamily}" SnapsToDevicePixels="True" UseLayoutRounding="True" TextOptions.TextFormattingMode="Display">
    <Grid Background="{DynamicResource Surface0Brush}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Border Background="{DynamicResource Surface1Brush}" BorderBrush="{DynamicResource Border0Brush}" BorderThickness="0,0,1,0">
            <DockPanel Margin="12,22,12,14">
                <Grid DockPanel.Dock="Top" Margin="5,0,0,25">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="36" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Image Source="/Assets/logo.png" Width="30" Height="30" RenderOptions.BitmapScalingMode="HighQuality" />
                    <StackPanel Grid.Column="1" Margin="7,0,0,0">
                        <TextBlock Text="{Binding AppName}" Foreground="{DynamicResource Text0Brush}" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap" />
                        <TextBlock Text="本机软件管控" Style="{StaticResource PgCaption}" FontSize="11" Margin="0,3,0,0" />
                    </StackPanel>
                </Grid>
                <Button DockPanel.Dock="Bottom" DataContext="{Binding AboutItem}" Content="{Binding Title}" Command="{Binding Command}" Style="{StaticResource PgNavButton}" Margin="0,18,0,0" />
                <ItemsControl ItemsSource="{Binding NavItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Title}" Command="{Binding Command}" Style="{StaticResource PgNavButton}" HorizontalAlignment="Stretch" Margin="0,2" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DockPanel>
        </Border>
        <Grid Grid.Column="1" Margin="26,23,26,20">
            <ContentControl Content="{Binding CurrentPage}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />
            <ItemsControl ItemsSource="{Binding Toasts}" VerticalAlignment="Bottom" HorizontalAlignment="Right" MaxWidth="380" Margin="0,0,0,10">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource Surface3Brush}" BorderBrush="{DynamicResource Border0Brush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,6,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="0,0,10,0" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsError}" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource DangerBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <Button Grid.Column="1" Style="{StaticResource PgIconButton}" Content="{StaticResource CloseIcon}" ToolTip="关闭提示" AutomationProperties.Name="关闭提示" Command="{Binding DataContext.DismissToastCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
        <Border Grid.Row="1" Grid.ColumnSpan="2" Background="{DynamicResource Surface1Brush}" BorderBrush="{DynamicResource Border0Brush}" BorderThickness="0,1,0,0" Padding="14,6">
            <TextBlock Text="{Binding FooterText}" Style="{StaticResource PgCaption}" FontSize="11" />
        </Border>
    </Grid>
</Window>
